# Dukascopy MCP Server — minimal dependencies
# All data fetching/decoding uses Python stdlib only (urllib, lzma, struct, etc.)
# NO pandas, numpy, requests, httpx, mcp, pydantic, or anything requiring Rust/C.
# MCP JSON-RPC protocol is implemented directly in server.py using starlette.

# Pure-Python HTTP server — no compilation needed, works in plain Termux
starlette>=0.41.0
uvicorn>=0.30.0
